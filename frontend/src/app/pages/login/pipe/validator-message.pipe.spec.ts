import { ValidatorMessagePipe } from './validator-message.pipe';

describe('ValidatorMessagePipe', () => {
  it('create an instance', () => {
    const pipe = new ValidatorMessagePipe();
    expect(pipe).toBeTruthy();
  });
});
